# Plan: HTML 贪吃蛇游戏

## Metadata
- date: 2026-04-09
- description: 在 games/ 文件夹内创建一个纯 HTML+CSS+JS 的贪吃蛇游戏，包含画布渲染、键盘控制、食物生成、碰撞检测、计分和游戏结束逻辑。
- status: in-progress

## Tasks

### [T01] 创建 games 目录和 HTML 骨架
- status: [x]
- description: 创建 games/ 目录，新建 games/snake.html，包含基本 HTML5 结构、<canvas> 元素、内联 <style> 区域和空的 <script> 区域。页面标题为"贪吃蛇"，canvas 居中显示，背景为深色。
- commit_message: feat: add games/snake.html with base HTML structure and canvas

### [T02] 实现游戏初始化与渲染循环
- status: [x]
- description: 在 script 区域定义常量（格子大小、列数、行数、初始速度），初始化蛇的位置（居中，长度3），实现 draw() 函数用 canvas 2D context 绘制网格背景和蛇身（圆角矩形），启动 setInterval 驱动游戏循环。
- commit_message: feat: implement game init and render loop

### [T03] 实现蛇的移动与键盘控制
- status: [x]
- description: 实现方向变量和 keydown 监听（ArrowUp/Down/Left/Right 及 WASD），每帧按方向移动蛇头，尾部跟随。禁止反向移动（如向右时不能直接向左）。
- commit_message: feat: implement snake movement and keyboard controls

### [T04] 实现食物生成与吃食逻辑
- status: [x]
- description: 随机在空白格子生成食物（红色圆形），蛇头碰到食物时：蛇长度+1（不移除尾部），重新生成食物，分数+10。在 canvas 上绘制食物。
- commit_message: feat: implement food spawning and eating logic

### [T05] 实现碰撞检测与游戏结束
- status: [ ]
- description: 每帧检测蛇头是否碰到墙壁或自身；碰到后停止游戏循环，在 canvas 中央绘制"Game Over"及最终分数，显示"按空格键重新开始"提示，监听空格键重置游戏状态。
- commit_message: feat: implement collision detection and game over screen

### [T06] 添加开始界面、计分板与速度递增
- status: [ ]
- description: 游戏启动时显示开始界面（标题、最高分、"按空格开始"），每吃5个食物速度提升5%（缩短 interval），在画布顶部实时显示当前分数和最高分（localStorage 持久化）。
- commit_message: feat: add start screen, scoreboard and speed progression
