---
description: search feedmepos GitHub organization using gh only
agent: build
---

Use the `gh` command to search GitHub organization `feedmepos` for repositories or code matching the user's query. Do not search, read, or inspect any local files. Only use `gh` to query GitHub and report the results.
